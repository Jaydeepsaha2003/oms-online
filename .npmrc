# Smooths installation across a multi-framework monorepo (NestJS + Vite + Prisma).
# Peer-dependency ranges across these ecosystems occasionally disagree on majors;
# this lets npm resolve a single working tree instead of hard-failing the install.
legacy-peer-deps=true
fund=false
audit=false
